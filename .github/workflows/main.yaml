on:
  push:
    branches:
      - mainzz
name: 🚀 Deploying to staging
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2.3.2

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.0.0
      with:
        server: ${{ secrets.SHELTA_PHILEOSOFT_FTP_SERVER }}
        username: ${{ secrets.SHELTA_PHILEOSOFT_FTP_USERNAME }}
        password: ${{ secrets.SHELTA_PHILEOSOFT_FTP_PASSWORD }}
        server-dir: /

